const tasks = [
    { id: 1, name: 'Fishing' },
    { id: 2, name: 'Shopping' },
    { id: 3, name: 'Swimming' }
  ];

function updateTask(id, newName) {
let newArray = tasks.slice(0)

let find = newArray.findIndex(item => item.id === id)

if (find !== -1) {}


  return newArray




}
