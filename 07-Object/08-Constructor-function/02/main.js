function Accumulator(input1,input2) {
    
this.currentValue = startingValue

this.read = function () {
    let newInput = +prompt('Enter here')
    this.currentValue += num}
    
this.show = function() {
         alert(this.currentValue)}
       
}
    
    let accumulator = new Accumulator (9)
    // console.log(accumulator.num1)
    // console.log(accumulator.num2)
    accumulator.read()
    accumulator.show()
